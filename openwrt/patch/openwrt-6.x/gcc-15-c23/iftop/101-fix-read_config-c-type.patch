--- a/cfgfile.c
+++ b/cfgfile.c
@@ -245,6 +245,6 @@
     }
 }
 
-int read_config(char *file, int whinge_on_error) {
+int read_config(const char *file, int whinge_on_error) {
     return read_config_file(file, whinge_on_error);
 }
